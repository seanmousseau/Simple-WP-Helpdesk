[pytest]
testpaths = scripts
python_files = test_*.py
python_functions = test_*

# Suppress "PytestUnknownMarkWarning" for our custom marks
markers =
    smoke: quick pre-push sanity check
    security: security-focused checks
    slow: slow tests (CDN, file I/O)

# Default options
addopts = --tb=short
